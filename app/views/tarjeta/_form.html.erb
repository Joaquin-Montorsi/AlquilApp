<%= form_with(model: tarjetum) do |form| %>
  <% if tarjetum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tarjetum.errors.count, "error") %> prohibited this tarjetum from being saved:</h2>

      <ul>
        <% tarjetum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nro_tarjeta, style: "display: block" %>
    <%= form.number_field :nro_tarjeta %>
  </div>

  <div>
    <%= form.label :fecha_caducidad, style: "display: block" %>
    <%= form.date_field :fecha_caducidad %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
