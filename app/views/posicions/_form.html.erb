<%= form_with(model: posicion) do |form| %>
  <% if posicion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(posicion.errors.count, "error") %> prohibited this posicion from being saved:</h2>

      <ul>
        <% posicion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :x, style: "display: block" %>
    <%= form.text_field :x %>
  </div>

  <div>
    <%= form.label :y, style: "display: block" %>
    <%= form.text_field :y %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
